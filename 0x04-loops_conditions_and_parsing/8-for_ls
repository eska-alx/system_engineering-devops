#!/usr/bin/env bash
# This script lists the contents of the current directory, displaying the part of the filename after the first dash (-).

for file in $(ls)
do
    # Skip hidden files
    if [[ $file != .* ]]; then
        # Extract the part of the filename after the first dash
        echo "${file#*-}"
    fi
done
